# SDKWork workspace metadata

Source-controlled agent workspace for `sdkwork-web-framework`.

- `skills/` — repository-local skills (see `README.md` in each folder).
- `plugins/` — repository-local plugins for SDKWork tooling.

Runtime user data belongs under `~/.sdkwork/`, not here.
