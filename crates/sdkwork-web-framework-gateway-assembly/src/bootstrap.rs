//! Generated gateway bootstrap for sdkwork-web-framework.
//! Regenerated by pnpm gateway:assembly:materialize when bootstrap contains todo!().

use axum::Router;
use sdkwork_web_framework_admin_repository_sqlx::AdminStorePool;

pub struct ApplicationAssembly {
    pub router: Router,
}

pub fn assemble_application_router(pool: AdminStorePool) -> ApplicationAssembly {
    let mut router = Router::new();
    router = router.merge(sdkwork_routes_web_framework_backend_api::gateway_mount(
        pool,
    ));
    ApplicationAssembly { router }
}
